#include <stdio.h>

int main(void){
    int n;                                              //
    scanf("%d", &n);                                    //
    int d[n];                                           //
    int y[n];                                           //
    int v_ini, v_min, v_max, t_max;                     //
    float k, CT, CA;                                    // lendo
    for (int i = 0; i < n; i++){                        // 
        scanf("%d", &d[i]);                             // a
    }                                                   //
    for (int i = 0; i < n; i++){                        // entrada
        scanf("%d", &y[i]);                             //
    }                                                   //
    scanf("%d %d %d %F", &v_ini, &v_min, &v_max, &k);   //
    scanf("%d %f", &t_max, &CT);                        //
    scanf("%f", &CA);                                   //

    
}